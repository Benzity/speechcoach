fastapi
uvicorn[standard]
sqlalchemy
pydantic[email]
python-multipart
python-dotenv
anthropic
pdfplumber
faster-whisper
# 0.10.30+ 에서 legacy `mediapipe.solutions` API가 제거됨 — vision_worker.py가 의존.
# Python 3.11~3.12에서 동작. Python 3.13/3.14는 wheel 부재.
mediapipe>=0.10.18,<0.10.30
librosa
ffmpeg-python
numpy
bcrypt
PyJWT
# 답변 영상 저장 암호화(AES-256-GCM)에 사용 — 안전성 확보조치 기준 제7조.
# 전이 의존성으로 들어오더라도 직접 사용하므로 명시한다.
cryptography
