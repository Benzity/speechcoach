[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# SPA 라우팅 fallback — /onboarding, /result/xxx 새로고침 시 404 방지
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
